# Cost Management Cloud 開発引き継ぎ（大塚さん案件）

このメモを最初に読んで、現状を把握してから開発を進めてください。

## プロジェクトの目的

- **クライアント**: 大塚さん向けの建設業向けクラウド原価管理システム
- **参考ソフト**: レッツ原価管理Go2（発注書→仕入伝票→原価明細の3層構造）。ただし設計思想は「人がやる必要のない手順は省く」
- **Claude Codeに移行した理由**: Replitのランニングコストが高いため、開発環境をローカル+Claude Codeに移したい
- **最終目的**: 大塚さんが実運用できる状態に仕上げる

## 技術構成

- pnpm workspace モノレポ
  - `artifacts/api-server` … Express 5（API）
  - `artifacts/genka-kanri` … React + Vite（フロント、Wouter / React Query / shadcn/ui / Recharts）
  - `lib/db` … Drizzle ORM + PostgreSQL スキーマ・接続
  - `lib/api-spec` / `lib/api-client-react` / `lib/api-zod` … OpenAPI + Orval codegen
- GitHub: `sola-oss/Cost-Management-Cloud`（**push するとReplit本番にも反映されるので注意**）

## ローカル起動方法（今日セットアップ済み）

- **DB**: ローカルPostgres。`DATABASE_URL=postgresql://i@localhost:5432/costmanagement`
  - Replitの本番DB(heliumdb)からダンプを移行済み（大塚邸データ入り、cost_itemsは0件）
- **api-server起動**: `artifacts/api-server` で
  `DATABASE_URL=postgresql://i@localhost:5432/costmanagement NODE_ENV=development PORT=3000 pnpm run dev`
- **フロント起動**: `artifacts/genka-kanri` で
  `PORT=5173 BASE_PATH=/ NODE_ENV=development pnpm run dev`
- `vite.config.ts` に `/api` → `localhost:3000` のプロキシ追加済み
- **注意**: Replit発祥のため `pnpm-workspace.yaml` で darwin系モジュールが overrides 除外されている。ローカルで足りないネイティブモジュール（例: `@rollup/rollup-darwin-arm64`）は都度入れる必要あり

## 開発の現状

画面作成はほぼ完了。以下すべて実装済み：
- 見積書、実行予算（見積取込ボタンあり）、仕入入力（実績原価入力・発注書取込あり）
- 予実管理（収支状況・予算残・粗利率）、工事台帳
- 粗利率は3階層で表示確認済み：ダッシュボード（平均）／工事一覧・工事台帳（工事ごと）／実行予算画面（行ごとの予定利益率）
- 仕入先マスタは登録済み（東北建材株式会社 など）
- 工種マスタ — 21件シード済み（建築・土木・設備・その他）
- **単価マスタ** — DB(`unit_prices`)・API(`/api/unit-prices`)・管理画面(`/master/unit-prices`) 新規実装済み
  - 仕入先 × 工種 × 品目 → 単価 の構造
  - テストデータ3件登録済み（東北建材: 構造用合板, 杉KD材, 石膏ボード）
- ※ 現状はダミーデータ。精度検証は実データ投入後

### 2025-06-05 実装分

**単価マスタ（②）**
- `lib/db/src/schema/unit-prices.ts` — テーブルスキーマ
- `artifacts/api-server/src/routes/unit-prices.ts` — CRUD API + フィルタ
- `artifacts/genka-kanri/src/pages/master/unit-prices.tsx` — 管理画面

**単価ピッカー（③入力改善）**
- `artifacts/genka-kanri/src/components/unit-price-picker.tsx` — 共通コンポーネント
- 適用先: 仕入入力(`purchases.tsx`)、発注書(`purchase-orders/index.tsx`)、実行予算(`budgets.tsx`)
- フロー: 仕入先選択 → 「単価選択」ボタン → 品目一覧から選択 → 品名・単位・単価・工種を自動補完

**仕入入力画面のUI改善**
- ヘッダー整理: 主要3項目（工事・仕入先・仕入日）のみ常時表示、残りは「詳細設定」折りたたみ
- 工事を先頭（最重要フィールド）に配置
- 仕入先未選択時は「単価選択」ボタン非表示
- 科目はコードなし名前のみ表示（外注費 / 材料費）
- 行の複製ボタン追加
- 単価ピッカー選択後に数量フィールドへ自動フォーカス
- 数量・単価フィールドにカンマ区切り表示（`NumberInput`コンポーネント）
- サイドバーのマスタ管理をデフォルト折りたたみ

### 2026-06-08 実装分（業者別原価管理の強化）

**全入力画面のカンマ区切り＋UI整理**
- 発注書・見積書・請求書・工事登録・工事詳細・実行予算に `NumberInput` 適用
- 工事登録: 請負金額テーブルを8行固定→1行＋追加ボタンに
- 見積書: 自社情報セクションを折りたたみ

**実行予算画面の改善**
- 編集セルの視認性向上（実行予算列をアンバー強調、入力欄として枠表示）
- KPIバー: 「利益」→「実績利益」、実績原価0時は「—」表示（誤解防止）
- 「予定利益/予算残」表示トグルを実機能化（飾りだった `displayMode`/`profitMode` を実装、出来高トグルは撤去）
- 工種マスタ・実行予算へ最短2クリックの導線追加（工事一覧に「実行予算」リンク、詳細の実行予算タブを直接遷移化）

**工種×仕入先での実績突合（業者別原価管理）**
- モニターAPI(`budget-items.ts /monitor`)の突合を工種コード→工種名ベースに修正（コードが空でも一致）。予算に無い工種の実績は「（予算なし）」行で必ず合計に含める
- 工種×仕入先の実績内訳(`costByWorkTypeVendor`)を返し、実行予算の行単位の消化率を「工種＋仕入先」で計算
- 原価モニターの予定利益のバグ修正（請負金額を工種名で合計するよう修正。以前は常に1行目を拾っていた）
- 実行予算: 仕入先未設定の警告バナー＋該当セルのハイライト

**仕入先入力の手間削減**
- 工種マスタに「標準仕入先」(`work_types.default_vendor_id`)を追加。見積取込時に工種名から自動セット
- 実行予算に「選択行へ仕入先を一括設定」機能
- 仕入入力の工種選択を検索可能・全工種表示に（`WorkTypeSelect`）。工種列を科目の隣へ移動
- 単価ピッカーに工種フィルタ追加（行の工種で初期絞り込み）

**その他**
- 仕入伝票の削除機能（一覧に削除ボタン、支払済・査定済は保護）
- テスト用に仕入先を11社に増やした

**実績原価の入口を仕入入力に一本化**
- 工事詳細の原価明細タブは閲覧専用化（直接登録を廃止、「仕入入力で登録」リンクに）
- 孤立していた `/projects/:id/costs/new` ページを削除
- 実績原価は ① 仕入入力 のみから登録される（原価の確認は原価明細タブ・モニター・収支レポート）

**実行予算の発注バグ修正**
- 一括発注で発注明細に工種IDが入らず「未分類（予算なし）」になっていたのを修正（`budget-items.ts`：工種名→ID解決）
- 発注書作成後に原価モニター・KPIが自動更新されない問題を修正（`budgets.tsx`：monitor/summaryをinvalidate）
- 既知の未対応（仕様判断待ち）：工事の請負金額(project) と 予算行の請負合計 の不一致 → KPI/合計の予定利益が二重値になる。下書き発注書を発注済計上。「集計区分:税抜」は飾りラベル。
- 運用ルール：同じ工種＋同じ仕入先は予算を1行で登録する（複数行に分けると予算残モードで重複表示が出る）

**実行予算の仕様判断対応（昨日の検査の続き）**
- 請負金額の不一致警告：予算行の請負合計が工事の請負金額とズレた時だけ赤い警告バナーを表示（正しいデータなら出ない）。予定利益が画面で二重値になる問題の安全網
- 発注済の集計を修正：「発注済・一部納品・完納」のみカウント（下書き・キャンセルは除外）。実行予算からの一括発注は最初から「発注済」状態で作成（作った瞬間に反映）
- 「集計区分：税抜」ラベルは飾りのまま残す（仕様判断で据え置き）

### 2026-06-09 実装分（工事一覧の検査 A・B・C＋粗利率の全画面統一）

**A：工事一覧の検索をサーバー側で実装**
- `projects.ts` 一覧GET：`search` で工事名・工事番号・得意先名を部分一致（ilike）。フロント `projects/index.tsx` に検索ボックス＋300msデバウンス

**B：予算消化率・実行予算が空の旧テーブルを読んでいたバグ修正**
- 旧 `budgets`（4区分・ほぼ空）→ 新 `budget_items.revised_budget` の合計を実行予算として読むよう修正
- 対象：工事一覧・工事詳細・サマリ(`projects.ts`)、ダッシュボード(`dashboard.ts`)
- 効果：大塚邸の消化率6.7%・実行予算¥6,000,000が正しく表示、ダッシュボードの予算超過リスクも機能

**C：粗利率を「予定ベース」に全画面統一＋計算式の明示**
- 粗利率 ＝（請負金額 − 実行予算）÷ 請負金額（計画段階の採算）。実行予算未設定の工事は「—」表示
- 工事一覧：粗利率を予定ベースに変更。ヘッダーに計算式ツールチップ（ℹアイコン）を追加
- 工事詳細：「予想粗利」カード（ヘッダー・収支状況タブの2か所）を「予定／実績」の2段表示に変更（予定50%・実績96.7%を両方表示しラベル明確化）。`/:id/summary` に `plannedGrossProfit`/`plannedGrossProfitRate` を追加
- ダッシュボード：平均粗利率を予定ベースに変更（実行予算が設定された工事のみで平均）
- ねらい：以前は「一覧50% / 詳細96.7% / ダッシュボード98.3%」と画面ごとに食い違っていたのを統一。詳細では実態（実績）も併記

### 2026-06-10 実装分（画面ごとの検査と修正：見積・発注・仕入入力・支払査定）

各画面を1つずつ検査（バグは先に報告→方針承認後に修正）。

**見積書**
- 印刷クラッシュ修正：有効期限が「見積日より1ヶ月」等の文章だと印刷の `fmtDate` が落ちていた（`estimates/print.tsx`）。日付形式でなければそのまま表示するよう防御的に修正

**発注書**
- 印刷を手動ボタンに統一（`purchase-orders/print.tsx`）。以前は開くと自動で `window.print()` が走り見積印刷と不統一だった
- 削除時の警告：紐づく仕入伝票がある発注書を削除しようとすると件数を警告（`purchase-orders.ts` の詳細取得に `linkedInvoiceCount` 追加、`detail.tsx` で警告文）。ブロックはしない

**仕入入力**
- 編集時の工事・仕入先をロック（`purchases.tsx`）。PATCHが projectId/vendorId を保存しないのに編集可能に見え、変更が黙って無視されていた → 編集時はグレーアウト＋「（編集中は変更不可）」。直す時は削除して作り直す運用（案A）
- 検査OK確認：合計はサーバー再計算、税は行ごとfloor、実績原価同期は重複なし、発注書から取込（未納品分のみ・PO納品数量/完納を自動更新・再取込防止・削除で復元）

**支払査定：二重査定の完全ガード**
- `purchase_invoices` に `assessed_at`（査定済み日時）を追加（スキーマ：`lib/db/.../purchase-invoices.ts`）
- 集計は未査定のみ対象（`payment-assessments.ts`）。条件に「査定済みも含める」チェックで訂正時は再表示可
- 査定確定時に対象仕入伝票へ印を付与、削除（査定由来の支払）時に印を戻す（`payments.ts`、取消は案A=期間×工事×仕入先で復元）
- ※ ローカルDBは手動 ALTER 済み。Supabase移行時は drizzle スキーマから `drizzle-kit push` で反映

## 残To Do

1. ~~単価マスタ~~ → ✅ 完了
2. ~~工種マスタのデータ登録~~ → ✅ 完了（21件シード済み）
3. ~~入力画面の操作性改善~~ → ✅ 完了（単価ピッカー + UI改善）
4. **テスト（実データ投入後）**: サンプル5件入力／見積→予算→実績の流れ確認／予実比較・粗利率の精度確認／使いにくい点の洗い出し
5. **単価マスタへの実データ投入** — 大塚さんからの単価表（Excel/CSV）を受け取り次第、登録する（CSV一括インポート機能は未実装、必要に応じて作る）

## ボトルネック

開発側の主要機能は完了。**最大のボトルネックは大塚側のデータ提供待ち**：
- 材料・外注業者の単価表の提供（未対応・最優先）→ 単価マスタに投入する
- サンプル工事5件（100万円以上）のデータ準備（現状ダミー1件のみ）
- レッツ原価の過去データ抽出

## 進め方の好み

- 確認質問は最小限、まとめて一気に進めたい
- 細かい確認のために手を止めず、必要な調査は自分で済ませて包括的に出力する
