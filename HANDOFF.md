# CMC 引き継ぎメモ（2026-07-29 時点）

新しいチャットでこのファイルを読ませれば続きから作業できます。

## プロジェクト
- **CMC = 原価管理クラウド**（建設業向け）／ `/Users/i/アプリ開発/Cost-Management-Cloud`（pnpm モノレポ）
  - フロント `artifacts/genka-kanri`（Vite + React + wouter + TanStack Query + Tailwind）
  - API `artifacts/api-server`（Express + Drizzle）／ スキーマ `lib/db/src/schema`
- DB: Supabase（プロジェクト "Cost Management Cloud" = `rnysqjnqdpdmaylojwqr`）
- デプロイ: Railway。**main への push で自動デプロイ**
- 本番: `cost-management-cloud-production.up.railway.app` ／ repo: `github.com/sola-oss/Cost-Management-Cloud`

## 重要な前提
- **おおつか様が本番を実利用中**。本番には実案件名・仕入先197社・社員27名の**実名**が入っている。
- **商談デモに本番URLを使わない**（守秘）。デモは必ずローカル（下記）。
- 標準手順：実装 → typecheck/build → ローカル確認 → デプロイ → URL確認

## デモ環境（商談用）
- デスクトップの **`CMCデモ起動.command`**（起動）と **`CMCデモ初期化.command`**（データ作り直し）
- DB `cmc_demo` ／ 投入スクリプト `lib/db/seed-demo.mjs`
- 架空の「株式会社みらい建設」。工事4件を意図的に4状態（順調／原価先行／出来高未入力／予算超過）に。
- ログイン `demo@mirai-kensetsu.example` / `MiraiDemo2026`
- 起動時に出る LAN URL で**同じWi-Fiのスマホから開ける**
- **デモの原価はすべて仕入伝票から発生させてある。** 原価の直接入力UIが無く、出面入力も未実装のため。

## 仕入の振り分け（この機能が中心）
下請から届く請求書・納品書を先にデータ化し、現場担当者が**納品書のかたまり単位**で工事を選ぶ仕組み。

**流れ**：事務が取り込む（AI読み取り or 手入力）→ **確認画面で中身を整える** → 送り先を選んで現場へ
→ 現場がブロックごとに工事を選ぶ → 事務に返す → 事務が確定 → 工事ごとに仕入伝票を生成

- 主なファイル: `artifacts/api-server/src/routes/{ai-extract,received-invoices}.ts`、
  `artifacts/genka-kanri/src/pages/received-invoices/{index,detail,editor,manual-entry}.tsx`、
  `lib/db/src/schema/received-invoices.ts`
- 入金・繰越などの行は `isNonPurchase` で原価から除外。返品・値引はマイナスの仕入行として計上。

### 2026-07-29 に入れたもの（commit 3ec6ca6 と 507ecb6・本番反映済み）
- **順番の是正**：取り込み直後は送らず確認画面へ。確認画面は 仕入先 → 明細 → 送り先 → 送信 の順。
  仕入先が未確定なら送信も確定もできない（以前は仕入先を確かめずに現場へ送れた）
- **下書きの編集画面**（`PATCH /api/received-invoices/:id`）。請求日・支払期日・請求額・明細に加え
  **科目と工種**を選べる。工種を渡していなかったため、振り分け経由の原価が全部
  「未分類（予算なし）」に落ちて工種別の予算残から漏れていた
- **仕入先のその場登録**。マスタに無い社名でも確認画面から登録して紐づけ。似た名前の既存を先に提示
  して二重登録を防ぐ。手入力もマスタに無い社名で起票できる
- **確定の二度押しで原価が倍になる不具合の修正**（実際に再現：11万円→22万円）。
  先に「確定済み」を書き込めた1本だけが伝票作成に進む。途中で失敗したら確定の印を戻す
- **複数現場での分担**：自分の分だけ返せる／返した人が選んだ行だけをブロック単位で固定／
  現場から返ってきた書類は工事変更不可／差し戻し（回答済みの印と固定を解除）
- **二重取り込みの警告**（同じ仕入先・請求日・請求額）。止めずに警告だけ（同額の請求が別々に届くため）
- **確定の取り消し**。伝票と原価を消して確認待ちへ戻し、備考に記録。支払済・査定済があるときは不可
- 一覧：支払期日の近い順／対応中・確定済のタブ／300件上限／KPIに下書きと確定前の未割当
- 請求額のラベルを「原本の今回ご請求額」→「AIが読み取った金額です」に。読み違えると原本に無い数字を
  原本の値だと言い切ることになる。合わないときは「明細の合計を入れる」を1押しで（自動計算にはしない）

### AI読み取り（`ai-extract.ts`・claude-sonnet-5）
- **本番で有効化済み**（Railwayに `ANTHROPIC_API_KEY` / `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` を設定）。
  原本画像は Supabase Storage のバケット `received-invoices`（非公開）へ
- **深さ（effort）を `AI_EXTRACT_EFFORT` で変更可・既定 medium。** 未指定だと既定の high で毎回
  いちばん深く考え、待ち時間が倍になっていた
- 実測（印字・3ページ33行）：high 87秒 / medium 46秒 / low 44秒で**読み取り結果は3つとも同一**。
  45秒前後は明細33行を書き出す時間そのもので、これ以上は縮まない
- **手書きは実用にならない。** 小川板金の実物で high 97秒でも金額を外す（¥40,500→¥40,800）。
  手書きは「AIを使わず手で入力する」のほうが速い
- **手書きPDFは1ビット白黒（FAXモード）だった。** 灰色が無く薄い線が消える。
  スキャナをグレースケール300dpiに変えるのがモデル変更より効く見込み（未検証・要おおつか様へ依頼）
- 費用の目安：印字1ページ3〜5円、3ページ10円前後（月200枚で1,000〜2,000円）
- 金額の検算：税抜も税額も読めなかった場合に素通りしていた穴を塞いだ（税込どうしでも突き合わせる）
- 和暦のヒントを追加（「令和8年」を「平成28年」と読んで2016年になる誤りへの対策）

## 本番DBに追加した列（すべて追加のみ・非破壊）
`received_invoice_items` に3列。Supabase MCP の apply_migration で適用済み。
```
work_type_id           工種。確定時に仕入伝票へ引き継ぐ
locked_at              返した人の分を固定する（複数現場での分担用）
assigned_by_staff_id   誰が工事を選んだか。固定する行を判別するため
```

## 未対応・保留
| 件 | 状態 |
|---|---|
| **7/27 おおつか様との打ち合わせで出た改善点** | **内容をまだ受け取っていない。次の作業** |
| おおつか様への連絡 | 改善がひと通り終わってからまとめて送る方針。伝える内容は下記 |
| **出面入力**（自社職人の人工） | Q15〜Q19の回答待ち。特に**職人単価をマスタに持ち現場に見せてよいか**が社内確認待ち |
| 同じブロックを2人が同時に選ぶと後勝ち | 実害小と判断して未対応 |
| 現場担当者が本番にログインできない | `staff_members` と `users` が別。(A)アカウント作成 (B)トークンURL の未決 |
| 手書き請求書の枚数 | 未調査。少なければAIの精度改善は不要という判断 |
| 実行予算の自動化2件 | 顧客マスタ到着後 |
| MF会計への仕訳連携 | **やるかどうか未決。** 画面の文言にMF連携を出さない（決まっていない仕組みの名前を見せない） |
| 提案資料 | 未着手 |

### おおつか様へ伝える予定の内容（まとめて連絡する）
- AI読み取りが使えるようになった（PDF・写真を選ぶと読み取って確認画面が開く）
- **手書きの請求書はAIに読ませず手入力のほうが速い**
- スキャナを白黒からグレースケール300dpiに変えると読み取りが良くなる見込み
- 確定を間違えても取り消せるようになった（これまでは直せなかった）
- 1枚あたり3〜10円かかる。枚数が読めないのでしばらく様子見

## 既知の注意点・地雷
- **共有 queryKey の形不一致**：vendors は共通フックで解消済み。マスタ系を複数ページで共有するときは
  必ず共通フックで「**1キー=1形**」に統一する。`/api/vendors` は `{items,total}` を返す（配列ではない）。
- **Railwayビルドがたまに失敗**（`tsc: not found`）。install が `NODE_ENV=production` で devDependencies を
  飛ばすのが原因。`railway.json` の buildCommand を `NODE_ENV=development pnpm install --no-frozen-lockfile --force && …`
  にして対処済み。再発したらまずこの行を確認。
- **Railwayの環境変数は保存しても効かないことがある。** Deployments に新しい行が出ていなければ Redeploy。
  プロジェクト共通（Shared Variables）に入れた場合はサービス側で参照しないと渡らない。
- **`lib/db` のスキーマを変えたら `pnpm run typecheck:libs` を先に走らせる。** APIは
  ビルド済みの `@workspace/db` を見るので、これを忘れると「そんな列は無い」と言われる。
- **`preview_start` が別アプリ（genka-cloud:3001）を掴む**ことがある。CMCは手動でサーバを立てる。
- **ローカルの `vite build` が失敗する**：`pnpm-workspace.yaml` で darwin 用ネイティブモジュールを除外して
  いるため（Replit/Linux時代の名残）。Railway（Linux）は影響なし。typecheck がローカルの合否判定。
- **`git stash` を使わない**：`attached_assets` の未追跡PDFと衝突して pop が失敗する事故が起きた。
- shadcn の `Input` は `display:flex`。`flex-1` と併用すると `min-width` 未指定で0まで潰れる。
- **shadcn の `Select` は検索時に SelectItem 以外の子（グループ見出し）を残す。** 該当が無い見出しだけが
  宙に浮くので、`select.tsx` 側で「後ろに項目が無い見出しは落とす」処理を入れてある。

## 作業のやり方（このプロジェクトのルール）
- 変更後は必ず typecheck ＋ build（フロント `artifacts/genka-kanri`、API `pnpm --filter @workspace/api-server run build`）。
  スキーマを触ったら `pnpm run typecheck:libs` も。
- ローカル確認は使い捨てDBではなくデモDB（`cmc_demo`）で通しで行い、**作ったテストデータは必ず消す**
  （商談デモに使うDBなので、実在の下請先名が残ると守秘の問題になる）。
- デプロイ = main へ push。DBの列追加があるときは **先に本番DBへ適用してから push**。
  反映確認はバンドルハッシュの変化と、配信バンドルに新しい文言が含まれるかで行う。
- 実顧客が本番利用中のため、DBを壊す操作・大きな変更は特に慎重に。
- 画面の文言に、決まっていない仕組みの名前を出さない（例：MF会計連携）。

## 次にやること
**2026-07-27 のおおつか様との打ち合わせで出た改善点への対応。**（内容はアイさんから受け取る予定）
